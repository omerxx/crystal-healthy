pipeline:
  # test:
  #   image: omerxx/dronesecrets
  #   pull: true
  #   secrets: 
  #     - testvar
  docker:
    image: plugins/docker
    repo: omerxx/dronesecrets
    tags: build
    secrets:
      - testvar

